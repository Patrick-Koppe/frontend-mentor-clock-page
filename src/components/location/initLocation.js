import { setLocation } from "./setLocation";

import './location.scss';

export function initLocation(locationObj, selector) {
   // set HTML for loaction and country
   setLocation(locationObj, selector)
}